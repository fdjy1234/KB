# 容器技術架構管理規範與作業程序

> **文件版本：** v1.0
> **建立日期：** 2026-04-30
> **技術基礎：** Rocky Linux 10 / Docker CE / Kubernetes (kubeadm v1.31+)
> **文件分類：** 機密 / 內部使用

---

## 目錄

1. [基礎映像檔管理](#一基礎映像檔管理)
2. [映像檔弱點掃描與風險管理](#二映像檔弱點掃描與風險管理)
3. [容器映像檔登錄管理程序](#三容器映像檔登錄管理程序)
4. [鏡像庫平台存取控管機制](#四鏡像庫平台存取控管機制)
5. [容器技術引入對開發流程的影響](#五容器技術引入對開發流程的影響)
6. [開發部署上線工具與作業程序](#六開發部署上線工具與作業程序)
7. [容器管理平台角色與權限控管](#七容器管理平台角色與權限控管)
8. [容器上線前壓力測試](#八容器上線前壓力測試)
9. [佐證資料清單與產出指引](#九佐證資料清單與產出指引)

---

## 一、基礎映像檔管理

### 1.1 Base Image 來源政策

| 類別 | 說明 |
|------|------|
| **允許來源** | Docker Official Images、Verified Publisher、Red Hat UBI (Rocky Linux 相容)、Google Distroless、Ubuntu Chiseled |
| **禁止來源** | 未驗證第三方映像、個人帳號發布映像、來源不明映像 |
| **驗證方式** | Docker Content Trust (DCT) / Cosign 簽章驗證、SHA256 Digest 固定比對 |
| **優先選擇** | `alpine`、`ubi-minimal`、`ubuntu-chiseled`（.NET 9 多階段建置使用） |
| **CRI 支援** | containerd（符合 Kubernetes CRI 標準） |

### 1.2 Base Image 更新頻率規範

```mermaid
gantt
    title Base Image 更新週期管理
    dateFormat YYYY-MM-DD
    section 定期更新
    每月安全性更新掃描           :a1, 2026-04-01, 30d
    每季強制版本升級評估 Q2      :a2, 2026-07-01, 30d
    section 緊急更新
    Critical CVE 72小時更新     :crit, b1, 2026-04-15, 3d
    section 版本保留
    保留近3個正式版本            :done, c1, 2026-01-01, 120d
```

| 更新觸發條件 | 處理時限 | 負責單位 |
|-------------|---------|---------|
| CVE Critical 漏洞公布 | **72 小時內**完成更新及重新建置 | 資安團隊 + DevOps |
| CVE High 漏洞公布 | **7 個工作天內** | DevOps 團隊 |
| CVE Medium 以下 | 每月例行更新週期處理 | DevOps 團隊 |
| 官方 EOL 公告 | 提前 **90 天**啟動遷移計畫 | 架構師 + DevOps |
| 例行定期更新 | 每月第一個週二（Patch Tuesday 對齊） | DevOps 團隊 |

### 1.3 版本保留策略

```mermaid
graph LR
    subgraph KEEP[版本保留規則]
        V1["目前版本 Current"] --> V2["前一版 N-1"]
        V2 --> V3["前二版 N-2"]
        V3 --> DEL["超過3版 自動清除"]
    end
    subgraph EXCEPTION[例外保留]
        PROD["生產環境對應版本 永久保留"]
        AUDIT["年度稽核指定版本 保留1年"]
    end
    style V1 fill:#2ecc71,color:#fff
    style V2 fill:#f39c12,color:#fff
    style V3 fill:#e67e22,color:#fff
    style DEL fill:#e74c3c,color:#fff
    style PROD fill:#3498db,color:#fff
    style AUDIT fill:#9b59b6,color:#fff
```

**版本保留數量規範：**

- **Base Image**：保留最近 **3** 個版本
- **Application Image**：保留最近 **5** 個版本
- **生產環境運行版本**：永久保留直至下架後 1 年
- **Release Candidate**：僅保留 **7 天**，測試完畢後清除

---

## 二、映像檔弱點掃描與風險管理

### 2.1 弱點掃描工具架構

```mermaid
graph TB
    subgraph SCAN_TOOLS[弱點掃描工具層]
        TRIVY["Trivy\nCI 整合 / SBOM 生成"]
        GRYPE["Grype + Syft\nSBOM CycloneDX"]
        DOCKLE["Dockle\n設定合規掃描"]
    end
    subgraph DB[漏洞資料庫]
        NVD["NVD 國家漏洞資料庫"]
        GHSA["GitHub Advisory DB"]
        OSV["OSV Database"]
    end
    subgraph TRIGGER[掃描觸發時機]
        CI_TRG["GitLab CI Pipeline\n每次 Build"]
        SCHED["排程掃描\n每日 02:00"]
        MANUAL["手動觸發\n緊急 CVE"]
    end
    CI_TRG --> TRIVY
    CI_TRG --> GRYPE
    SCHED --> TRIVY
    SCHED --> DOCKLE
    MANUAL --> TRIVY
    TRIVY --> NVD
    TRIVY --> GHSA
    GRYPE --> OSV
    style TRIVY fill:#326ce5,color:#fff
    style NVD fill:#c0392b,color:#fff
```

### 2.2 弱點掃描流程與阻斷機制

```mermaid
graph TD
    START["Docker Build 完成"] --> SCAN["執行 Trivy + Grype 掃描"]
    SCAN --> SBOM["生成 SBOM 報告\nCycloneDX / SPDX 格式"]
    SBOM --> CHECK{"弱點等級判斷"}
    CHECK -->|"CRITICAL"| BLOCK["BLOCK\n阻止推送 Harbor\n自動開立 GitLab Issue\n通知資安團隊"]
    CHECK -->|"HIGH"| REVIEW["REVIEW\n暫停部署\n7天內修補計畫或豁免申請"]
    CHECK -->|"MEDIUM"| TICKET["TICKET\n允許部署至測試環境\n列入月度修補清單"]
    CHECK -->|"LOW / INFO"| PASS["PASS\n記錄至漏洞追蹤系統\n允許部署"]
    BLOCK --> FIX["開發團隊修補"]
    FIX --> RESCAN["重新掃描驗證"]
    RESCAN --> CHECK
    REVIEW --> WAIVER{"豁免申請?"}
    WAIVER -->|"核准"| DEPLOY_STG["部署至 Staging\n附加豁免標籤"]
    WAIVER -->|"拒絕"| FIX
    PASS --> SIGN["Cosign 簽章映像檔"]
    DEPLOY_STG --> SIGN
    TICKET --> SIGN
    SIGN --> PUSH["推送至 Harbor Registry"]
    style BLOCK fill:#e74c3c,color:#fff
    style REVIEW fill:#e67e22,color:#fff
    style TICKET fill:#f39c12,color:#fff
    style PASS fill:#27ae60,color:#fff
    style SIGN fill:#3498db,color:#fff
```

### 2.3 風險評估矩陣

| 嚴重等級 | CVSS 分數 | 處置 SLA | 允許部署環境 | 通知對象 |
|---------|----------|---------|-------------|---------|
| **CRITICAL** | 9.0 – 10.0 | **72 小時** | 全面阻止 | CISO、資安長、架構師 |
| **HIGH** | 7.0 – 8.9 | **7 天** | 限 DEV 環境 | 資安團隊、DevOps Lead |
| **MEDIUM** | 4.0 – 6.9 | **30 天** | DEV + STG | DevOps 團隊 |
| **LOW** | 0.1 – 3.9 | **90 天** | 全環境 | 紀錄留存 |
| **NONE** | 0.0 | 無需處置 | 全環境 | — |

### 2.4 多層防護機制

```mermaid
graph LR
    subgraph LAYERS[多層防護機制]
        L1["Layer 1\nGitLab CI Gate\n掃描不通過禁止推送"]
        L2["Layer 2\nHarbor Policy\n未簽章映像拒絕 Pull"]
        L3["Layer 3\nKyverno Admission Controller\n部署時驗證簽章"]
        L4["Layer 4\nFalco Runtime Security\n運行時異常偵測"]
        L1 --> L2 --> L3 --> L4
    end
    style L1 fill:#e74c3c,color:#fff
    style L2 fill:#e67e22,color:#fff
    style L3 fill:#f39c12,color:#fff
    style L4 fill:#27ae60,color:#fff
```

**Kyverno Policy 強制規則：**

- 禁止使用未指定 digest 的 `latest` tag
- 強制驗證 Cosign 簽章
- 禁止 `privileged: true` 容器
- 強制設定 `readOnlyRootFilesystem: true`
- 移除不必要的 Linux Capabilities

---

## 三、容器映像檔登錄管理程序

### 3.1 映像檔生命週期管理流程

```mermaid
stateDiagram
    [*] --> Developing : 開發人員建立 Dockerfile
    Developing --> LocalTest : docker build
    LocalTest --> CIScan : git push to GitLab
    CIScan --> ScanFail : 發現 Critical 或 High 漏洞
    ScanFail --> Developing : 回退修補
    CIScan --> PendingReview : 掃描通過
    PendingReview --> Rejected : 審核未通過
    Rejected --> Developing : 重新開發
    PendingReview --> Signed : Cosign 簽章完成
    Signed --> RegDev : 推送至 DEV Harbor
    RegDev --> RegStg : QA 驗證通過晉版
    RegStg --> StressTesting : 部署至 Staging 環境
    StressTesting --> RegProd : 壓力測試通過 變更核准
    RegProd --> Retired : 新版上線 舊版標記廢棄
    Retired --> [*] : 保留期限到期後清除
```

### 3.2 映像檔版本命名規範

```
格式：{harbor-host}/{project}/{image-name}:{major}.{minor}.{patch}-{build-number}

範例：
harbor.internal/backend/api-service:2.3.1-20260430
harbor.internal/frontend/web-portal:1.5.0-20260430
harbor.internal/infra/dotnet9-chiseled:9.0.4-20260428

Tag 規範：
vX.Y.Z-YYYYMMDD  → 正式版本（推送至 PROD Project）
vX.Y.Z-rc.N      → 候選版本（僅限 STG）
vX.Y.Z-dev.N     → 開發版本（僅限 DEV）
latest           → 禁止在 PROD 環境使用
```

### 3.3 映像檔登錄作業程序

```mermaid
sequenceDiagram
    participant DEV as 開發人員
    participant GIT as GitLab Repository
    participant CI as GitLab CI Pipeline
    participant SCAN as Trivy 掃描引擎
    participant APR as 審核人員
    participant REG as Harbor Registry
    participant K8S as Kubernetes Cluster

    DEV->>GIT: git push / Create MR
    GIT->>CI: 觸發 CI Pipeline
    CI->>CI: docker build (Kaniko)
    CI->>SCAN: 執行 Trivy 掃描
    SCAN-->>CI: 掃描報告 SBOM
    alt 掃描未通過 Critical/High
        CI-->>DEV: Pipeline 失敗通知
        DEV->>DEV: 修補漏洞後重新推送
    else 掃描通過
        CI->>APR: 通知審核 含掃描報告
        APR->>CI: 核准部署
        CI->>CI: Cosign 映像簽章
        CI->>REG: docker push with digest
        REG-->>CI: 推送成功
        CI->>K8S: 更新 Helm values.yaml via ArgoCD
        K8S->>REG: Pull Image 驗證 Cosign 簽章
        K8S-->>CI: Rolling Update 完成
        CI-->>DEV: 部署成功通知
    end
```

### 3.4 映像檔登錄管理紀錄欄位規範

| 欄位 | 說明 | 範例 |
|------|------|------|
| 映像名稱 | 完整 Harbor image path | `harbor.internal/backend/api:2.3.1` |
| SHA256 Digest | 不可竄改的唯一識別 | `sha256:abc123...` |
| 建置時間 | GitLab CI Pipeline 完成時間 | `2026-04-30T14:30:00+08:00` |
| 建置人員 | CI Service Account | `gitlab-ci-bot` |
| 基底映像 | Base image + digest | `ubi-minimal:9.4@sha256:xyz...` |
| 掃描結果 | 各等級漏洞數量 | `C:0 H:0 M:3 L:12` |
| 掃描工具版本 | 確保可重現性 | `trivy:0.51.0` |
| 簽章狀態 | Cosign 驗證狀態 | `SIGNED / UNSIGNED` |
| 部署環境 | 允許部署的環境 | `DEV / STG / PROD` |
| 核准人員 | 審核通過的負責人 | `arch-lead@internal` |

---

## 四、鏡像庫平台存取控管機制

### 4.1 Harbor Registry 架構設計

```mermaid
graph TB
    subgraph OUTER[外部存取區]
        DEV_PC["開發人員工作站\nWindows 11 + Hyper-V"]
        CI_RUNNER["GitLab CI Runner"]
    end
    subgraph DMZ[DMZ 管理網段]
        PROXY["Nginx Reverse Proxy\nTLS Termination"]
        APISIX_GW["APISIX API Gateway\nWAF / Auth"]
    end
    subgraph HARBOR_ZONE[Harbor 內部網段 192.168.250.10]
        HARBOR["Harbor Registry\nVM1 管理節點"]
        subgraph PROJECTS[Project 分區]
            PROD_PRJ["prod/\n最嚴格權限"]
            STG_PRJ["staging/\n受控存取"]
            DEV_PRJ["dev/\n開發人員存取"]
            BASE_PRJ["base/\n架構師才能寫入"]
        end
    end
    subgraph K8S_ZONE[Kubernetes 叢集 192.168.250.20]
        PROD_K8S["Production Namespace"]
        STG_K8S["Staging Namespace"]
        DEV_K8S["Dev Namespace"]
    end
    DEV_PC -->|"HTTPS + IP 白名單"| APISIX_GW
    CI_RUNNER -->|"HTTPS + Robot Account + IP 白名單"| APISIX_GW
    APISIX_GW --> PROXY
    PROXY --> HARBOR
    HARBOR --> PROJECTS
    PROD_K8S -->|"Pull Only Service Account"| PROD_PRJ
    STG_K8S -->|"Pull Only"| STG_PRJ
    DEV_K8S -->|"Pull Only"| DEV_PRJ
    style HARBOR fill:#326ce5,color:#fff
    style PROD_PRJ fill:#e74c3c,color:#fff
    style STG_PRJ fill:#f39c12,color:#fff
    style DEV_PRJ fill:#27ae60,color:#fff
    style APISIX_GW fill:#e74c3c,color:#fff
```

### 4.2 存取控管機制

**IP 白名單規則**

| 來源 | 允許存取 Project | 存取類型 |
|------|----------------|---------|
| 辦公室 VPN / Hyper-V 管理網段 | dev/、staging/ | Push / Pull |
| GitLab CI Runner 固定 IP | 全部 Project | Push（Robot Account） |
| Kubernetes Node IP（192.168.250.20） | 對應環境 Project | Pull Only |
| 管理員跳板機 IP | 全部含 prod/base | 管理操作 |
| 其他 IP | 全部拒絕 | — |

**帳號權限矩陣（Harbor RBAC）**

| 角色 | base/ | prod/ | staging/ | dev/ | 系統管理 |
|------|-------|-------|----------|------|---------|
| **系統管理員** | R/W | R/W | R/W | R/W | 是 |
| **資深架構師** | R/W | R | R/W | R/W | 否 |
| **DevOps 工程師** | R | R | R/W | R/W | 否 |
| **開發人員** | R | 無 | R | R/W | 否 |
| **CI Robot Account** | R/W | R/W | R/W | R/W | 否 |
| **K8S Service Account** | R | R（對應環境）| R | R | 否 |
| **稽核帳號** | R 唯讀 | R 唯讀 | R 唯讀 | R 唯讀 | 否 |

> R = Read (Pull)、W = Write (Push)

### 4.3 安全稽核與監控

```mermaid
graph LR
    HARBOR2["Harbor Registry"] -->|"存取日誌"| SIEM["ELK Stack\nLog 集中管理"]
    HARBOR2 -->|"異常告警"| ALERT["告警系統\nGrafana Alerting"]
    subgraph MONITORS[監控告警項目]
        A1["未授權存取嘗試"]
        A2["異常大量 Pull"]
        A3["非工作時間推送"]
        A4["新增高權限帳號"]
        A5["映像檔被異常刪除"]
    end
    SIEM --> A1
    SIEM --> A2
    SIEM --> A3
    SIEM --> A4
    SIEM --> A5
    style HARBOR2 fill:#326ce5,color:#fff
    style SIEM fill:#8e44ad,color:#fff
    style ALERT fill:#e74c3c,color:#fff
```

---

## 五、容器技術引入對開發流程的影響

### 5.1 開發流程對比

```mermaid
graph TB
    subgraph BEFORE[傳統部署流程 導入前]
        B1["程式開發"] --> B2["打包 WAR/JAR"]
        B2 --> B3["上傳至 AP Server"]
        B3 --> B4["手動修改設定"]
        B4 --> B5["重啟服務"]
        B5 --> B6["人工驗證"]
    end
    subgraph AFTER[容器化流程 導入後]
        A1["程式開發"] --> A2["Dockerfile 撰寫\n支援 Multi-stage .NET 9"]
        A2 --> A3["GitLab CI Build + Trivy 掃描"]
        A3 --> A4["Image Push to Harbor"]
        A4 --> A5["ArgoCD 自動同步"]
        A5 --> A6["K8s Rolling Update\ncontainerd CRI"]
        A6 --> A7["Prometheus 健康監控"]
    end
```

### 5.2 系統更新修補程序（容器化版本）

```mermaid
graph TD
    TRIGGER["更新觸發\nCVE通報 / 定期排程 / 需求變更"]
    TRIGGER --> TYPE{"更新類型"}
    TYPE -->|"緊急安全修補 Critical CVE"| EMERGENCY["緊急修補流程"]
    TYPE -->|"一般功能更新"| NORMAL["標準變更流程"]
    TYPE -->|"Base Image 更新"| BASE["基底映像更新流程"]
    EMERGENCY --> E1["資安通知 架構師 + CISO"]
    E1 --> E2["建立緊急分支 hotfix/CVE-XXXX"]
    E2 --> E3["修改 Dockerfile 升級受影響套件"]
    E3 --> E4["GitLab CI 緊急掃描驗證"]
    E4 --> E5{"掃描通過?"}
    E5 -->|"否"| E3
    E5 -->|"是"| E6["緊急 CAB 審核 2小時內"]
    E6 --> E7["生產環境部署"]
    E7 --> E8["發布事後報告"]
    NORMAL --> N1["開發 MR Code Review"]
    N1 --> N2["GitLab CI Pipeline Build + Test + Trivy"]
    N2 --> N3["推送至 Harbor DEV Project"]
    N3 --> N4["ArgoCD 自動部署 DEV"]
    N4 --> N5["QA 功能測試"]
    N5 --> N6["晉升 STG 環境"]
    N6 --> N7["壓力測試 + UAT"]
    N7 --> N8["CAB 變更審核"]
    N8 --> N9["生產環境 Blue-Green 部署"]
    BASE --> B_1["月度排程觸發"]
    B_1 --> B_2["更新 Dockerfile FROM tag"]
    B_2 --> B_3["重新建置所有相依映像"]
    B_3 --> B_4["全量 Trivy 掃描驗證"]
    B_4 --> B_5["回歸測試"]
    B_5 --> B_6["批次晉版至生產"]
    style EMERGENCY fill:#e74c3c,color:#fff
    style NORMAL fill:#3498db,color:#fff
    style BASE fill:#27ae60,color:#fff
```

---

## 六、開發部署上線工具與作業程序

### 6.1 工具鏈架構圖

```mermaid
graph TB
    subgraph DEV_TOOLS[開發工具層]
        IDE["VS Code / Rider\n+ Docker Extension"]
        DOCKERFILE["Dockerfile\nMulti-stage .NET 9\nUbuntu Chiseled"]
        GIT2["GitLab Repository\n192.168.250.10"]
    end
    subgraph CI_TOOLS[CI/CD 工具層]
        GITLAB_CI["GitLab CI\n.gitlab-ci.yml"]
        KANIKO["Kaniko\n無 Docker Daemon 建置"]
        TRIVY3["Trivy 掃描\nSBOM 生成"]
        COSIGN2["Cosign 簽章"]
        ANSIBLE2["Ansible Playbook\n自動化組態管理"]
    end
    subgraph REGISTRY_TOOLS[Registry 工具層]
        HARBOR3["Harbor Registry\nVM1 192.168.250.10"]
        NOTARY2["Notary V2\n信任管理"]
    end
    subgraph DEPLOY_TOOLS[部署工具層]
        ARGO2["ArgoCD GitOps"]
        HELM2["Helm Charts\nK8s 套件管理"]
        KUSTOMIZE2["Kustomize\n環境差異化設定"]
        METALB["MetalLB\nL4 Load Balancer"]
        VAULT["Vault + ESO\n機密管理"]
    end
    subgraph RUNTIME2[執行環境]
        K8S4["Kubernetes kubeadm v1.31+\nVM2 192.168.250.20"]
        CONTAINERD["containerd CRI"]
        CILIUM["Cilium eBPF CNI\nIdentity-based 安全"]
        KYVERNO2["Kyverno Policy Engine"]
        FALCO2["Falco Runtime Security"]
    end
    subgraph OBS2[可觀測性]
        PROM2["Prometheus + Grafana\nTCP BBR 效能監控"]
        ELK2["ELK Stack\nLog 集中管理"]
        VELERO["Velero DR/Backup"]
    end
    DEV_TOOLS --> CI_TOOLS
    CI_TOOLS --> REGISTRY_TOOLS
    REGISTRY_TOOLS --> DEPLOY_TOOLS
    DEPLOY_TOOLS --> RUNTIME2
    RUNTIME2 --> OBS2
    style HARBOR3 fill:#326ce5,color:#fff
    style K8S4 fill:#326ce5,color:#fff
    style ARGO2 fill:#ef7b4d,color:#fff
    style CILIUM fill:#f5a623,color:#fff
    style FALCO2 fill:#00b4c8,color:#fff
```

### 6.2 GitOps 部署作業程序

```mermaid
sequenceDiagram
    participant DEV as 開發人員
    participant GITLAB as GitLab App Repo
    participant INFRA as GitLab Infra/Config Repo
    participant CI2 as GitLab CI Pipeline
    participant HARBOR4 as Harbor Registry
    participant ARGO3 as ArgoCD
    participant K8S5 as Kubernetes

    DEV->>GITLAB: git push feature branch / MR
    GITLAB->>CI2: 觸發 CI Pipeline
    CI2->>CI2: Kaniko Build
    CI2->>CI2: Trivy 掃描 + SBOM
    CI2->>HARBOR4: Push Image with Digest
    CI2->>INFRA: 更新 Helm values.yaml image tag
    INFRA->>ARGO3: Git Webhook 通知變更
    ARGO3->>INFRA: Pull 最新 Manifest
    ARGO3->>K8S5: Apply Manifest
    K8S5->>HARBOR4: Pull Image 驗證 Cosign 簽章
    K8S5->>K8S5: Rolling Update 零停機
    K8S5-->>ARGO3: 回報部署狀態
    ARGO3-->>DEV: 部署成功/失敗通知
```

### 6.3 環境晉升程序

```mermaid
graph LR
    DEV_ENV["DEV 開發環境\n自動部署\n任何 MR merge"]
    STG_ENV["STAGING 測試環境\n手動核准\n每週一次"]
    PROD_ENV["PRODUCTION 生產環境\n變更審核\n排程發布"]
    DEV_ENV -->|"QA 通過 掃描無 Critical/High MR 核准"| STG_ENV
    STG_ENV -->|"UAT 通過 壓力測試通過 CAB 核准 回滾計畫確認"| PROD_ENV
    style DEV_ENV fill:#3498db,color:#fff
    style STG_ENV fill:#f39c12,color:#fff
    style PROD_ENV fill:#e74c3c,color:#fff
```

---

## 七、容器管理平台角色與權限控管

### 7.1 Kubernetes RBAC 角色架構

```mermaid
graph TB
    subgraph CLUSTER[Cluster Level]
        CA["cluster-admin\n僅限緊急使用 需雙人授權"]
        PA["platform-admin\n基礎設施管理 節點/命名空間"]
    end
    subgraph NS[Namespace Level]
        NA["namespace-admin\n特定命名空間完整管理"]
        DP["deployer\n部署更新 Deployment"]
        DR["developer\n唯讀 + kubectl logs/exec"]
        RO["readonly\n僅查看資源狀態"]
        MO["monitor\n僅 metrics 讀取"]
    end
    subgraph SA[Service Account]
        CI_SA2["gitlab-ci-sa\nGitLab CI Pipeline"]
        ARGO_SA["argocd-sa\nArgoCD 專用"]
        APP_SA["app-sa\n應用程式使用"]
    end
    CA -->|"最高權限 嚴格管控"| PA
    PA --> NA
    NA --> DP
    DP --> DR
    DR --> RO
    style CA fill:#e74c3c,color:#fff
    style PA fill:#e67e22,color:#fff
    style NA fill:#f39c12,color:#fff
    style DP fill:#27ae60,color:#fff
    style CI_SA2 fill:#3498db,color:#fff
    style ARGO_SA fill:#9b59b6,color:#fff
```

### 7.2 各角色權限明細

| 角色 | Pods | Deployments | Services | Secrets | ConfigMaps | Nodes | Namespaces |
|------|------|-------------|----------|---------|------------|-------|------------|
| **cluster-admin** | CRUD | CRUD | CRUD | CRUD | CRUD | CRUD | CRUD |
| **platform-admin** | CRUD | CRUD | CRUD | R | CRUD | R | CRUD |
| **namespace-admin** | CRUD | CRUD | CRUD | CRUD | CRUD | — | — |
| **deployer** | R/D | R/U | R | — | R/U | — | — |
| **developer** | R/Exec | R | R | — | R | — | — |
| **readonly** | R | R | R | — | R | — | — |
| **gitlab-ci-sa** | CRUD | R/U | R | — | R/U | — | — |

> CRUD = Create/Read/Update/Delete、R = Read Only、U = Update、D = Delete

### 7.3 特權存取管理流程

```mermaid
graph TD
    REQ["人員申請提高權限"] --> JUSTIFY["填寫申請單\n說明業務理由\n預計使用時間"]
    JUSTIFY --> APPROVE{"主管 + 資安\n雙重審核"}
    APPROVE -->|"拒絕"| REJECT["通知申請人\n紀錄拒絕原因"]
    APPROVE -->|"核准"| TEMP["授予臨時權限\nMax 4小時"]
    TEMP --> MONITOR3["全程操作錄影\nkubectl audit log"]
    MONITOR3 --> EXPIRE["時限到期\n自動撤銷權限"]
    EXPIRE --> REVIEW3["操作紀錄審查\n存檔90天"]
    style APPROVE fill:#f39c12,color:#fff
    style TEMP fill:#e74c3c,color:#fff
    style REJECT fill:#95a5a6,color:#fff
```

### 7.4 Kubernetes 安全基準設定（CIS Benchmark）

依據 **CIS Kubernetes Benchmark** 及 **NSA Kubernetes Hardening Guide** 進行設定：

| 設定項目 | 設定值 | 說明 |
|---------|-------|------|
| API Server 匿名認證 | `--anonymous-auth=false` | 禁止匿名存取 |
| RBAC 授權模式 | `--authorization-mode=RBAC,Node` | 啟用 RBAC |
| Audit Log | `--audit-log-path=/var/log/audit.log` | 完整稽核日誌 |
| etcd 加密 | `--encryption-provider-config` | Secret 靜態加密 |
| Pod Security Standards | `Restricted` mode（PROD） | 限制特權容器 |
| Network Policy | Default Deny All（Cilium eBPF） | 最小網路存取原則 |
| Kubelet 匿名認證 | `authentication.anonymous.enabled=false` | 禁止匿名存取 |
| TCP BBR | `net.core.default_qdisc=fq` | 核心效能優化 |
| Vault + ESO | External Secrets Operator | 機密不落地 K8s Secret |

---

## 八、容器上線前壓力測試

### 8.1 壓力測試流程

```mermaid
graph TD
    START3["容器部署至 Staging 完成"] --> PLAN["壓力測試計畫制定\n確認測試目標 指標 情境"]
    PLAN --> T1["基準測試 Baseline Test\n正常負載 10分鐘"]
    T1 --> T2["負載測試 Load Test\n預期峰值 x1.0 到 x1.5"]
    T2 --> T3["壓力測試 Stress Test\n逐步增加至系統極限"]
    T3 --> T4["突波測試 Spike Test\n瞬間 10x 流量衝擊"]
    T4 --> T5["耐久測試 Soak Test\n持續 2小時穩定負載"]
    T5 --> T6["恢復測試 Recovery Test\nHPA 自動 Scaling 及恢復驗證"]
    T6 --> EVAL{"通過驗收標準?"}
    EVAL -->|"通過"| REPORT_OK["產出測試報告\n標記 PASS"]
    EVAL -->|"未通過"| REPORT_FAIL["產出失敗報告\n開立優化工單"]
    REPORT_OK --> CAB2["提交 CAB 變更審核"]
    CAB2 --> PROD_DEPLOY["生產環境部署\nBlue-Green"]
    REPORT_FAIL --> OPT["效能優化\n調整資源限制 程式碼優化"]
    OPT --> T1
    style REPORT_OK fill:#27ae60,color:#fff
    style REPORT_FAIL fill:#e74c3c,color:#fff
    style PROD_DEPLOY fill:#3498db,color:#fff
```

### 8.2 壓力測試工具與指標

| 測試類型 | 使用工具 | 關鍵指標 |
|---------|---------|---------|
| HTTP 負載測試 | **k6** / **Locust** | RPS、P95/P99 Latency、Error Rate |
| API 壓力測試 | **JMeter** / **Gatling** | Throughput、Response Time |
| 容器資源壓力 | **stress-ng** | CPU / Memory / Network 使用率 |
| Kubernetes 擴展測試 | **kube-burner** | HPA 觸發時間、Pod 啟動時間 |
| 網路效能測試 | **iperf3**（Cilium eBPF 驗證） | 封包遺失率、頻寬 |

### 8.3 驗收標準（Production Gate Criteria）

| 指標 | 驗收標準 | 量測方式 |
|------|---------|---------|
| 回應時間 P95 | **< 500ms** | k6 metrics |
| 回應時間 P99 | **< 1,000ms** | k6 metrics |
| 錯誤率 | **< 0.1%** | HTTP 5xx / Total |
| CPU 使用率（峰值） | **< 80%** | Prometheus |
| Memory 使用率（峰值） | **< 85%** | Prometheus |
| HPA 擴展觸發時間 | **< 60 秒** | Kubernetes Event |
| Pod 啟動時間（containerd） | **< 30 秒** | kubectl rollout |
| 服務可用性（Soak Test） | **> 99.9%** | Uptime 監控 |

### 8.4 壓力測試紀錄表單範例

```
測試報告編號：PT-2026-0430-001
測試日期：2026-04-30
測試環境：Staging K8S Node (VM2 192.168.250.20)
          4 vCPU, 4GB RAM, containerd CRI, Cilium CNI
測試對象：harbor.internal/backend/api-service:2.3.1-20260430
測試工具：k6 v0.50.0 + Grafana Dashboard

測試結果摘要：
┌────────────────────────────────────────────────┐
│ 指標              │ 結果      │ 標準     │ 狀態 │
├────────────────────────────────────────────────┤
│ P95 Latency       │ 312ms     │ <500ms   │  PASS │
│ P99 Latency       │ 748ms     │ <1000ms  │  PASS │
│ Error Rate        │ 0.02%     │ <0.1%    │  PASS │
│ Peak CPU          │ 72%       │ <80%     │  PASS │
│ Peak Memory       │ 68%       │ <85%     │  PASS │
│ HPA Scale Time    │ 45s       │ <60s     │  PASS │
│ Soak Availability │ 99.96%    │ >99.9%   │  PASS │
└────────────────────────────────────────────────┘

結論：通過所有驗收標準，核准晉升至生產環境
核准人員：[架構師簽名]  日期：2026-04-30
```

---

## 九、佐證資料清單與產出指引

### 9.1 佐證資料對應表

| # | 佐證資料 | 負責單位 | 產出頻率 | 保存年限 |
|---|---------|---------|---------|---------|
| 1 | Base Image 版本清單及官方最新版對比表 | DevOps | 每月 | 3 年 |
| 2 | Trivy 工具設定文件及 GitLab CI 整合說明 | 資安 + DevOps | 版本變更時 | 5 年 |
| 3 | 弱點掃描報告（近一年，含 SBOM） | 資安 | 每次掃描 | 3 年 |
| 4 | 弱點風險評估及修補紀錄（GitLab Issues） | 資安 | 有漏洞時 | 3 年 |
| 5 | 容器映像檔版本管控規範（本文件） | 架構師 | 年度審查 | 永久 |
| 6 | Harbor 映像檔登錄管理程序及維護紀錄 | DevOps | 每次操作 | 3 年 |
| 7 | Harbor 帳號權限清單（季度審查截圖） | 資安 + DevOps | 季度 | 3 年 |
| 8 | Harbor 存取日誌 + ELK 異常事件報告 | 資安 | 持續 | 1 年 |
| 9 | k6 壓力測試報告（每次上線前） | QA + DevOps | 每次部署 | 3 年 |
| 10 | Kubernetes RBAC 設定及 Kyverno Policy 紀錄 | DevOps | 每次變更 | 3 年 |
| 11 | CAB 變更審核紀錄 | 變更管理 | 每次變更 | 5 年 |
| 12 | CIS Kubernetes Benchmark 稽核報告 | 資安 | 季度 | 3 年 |
| 13 | Velero 備份驗證及 DR 演練紀錄 | DevOps | 每月 | 3 年 |

### 9.2 年度稽核準備索引

```mermaid
graph TD
    ROOT["年度稽核準備清單"]
    ROOT --> IMG["映像檔管理"]
    ROOT --> SEC["安全掃描"]
    ROOT --> ACC["存取控管"]
    ROOT --> DEP["部署紀錄"]
    ROOT --> COMP["合規設定"]
    IMG --> IMG1["Base Image 來源證明"]
    IMG --> IMG2["版本更新紀錄 x12個月"]
    IMG --> IMG3["廢棄映像清除紀錄"]
    SEC --> SEC1["每日 Trivy 掃描報告彙總"]
    SEC --> SEC2["CVE 修補追蹤清單"]
    SEC --> SEC3["豁免申請及核准紀錄"]
    ACC --> ACC1["Harbor 帳號異動紀錄"]
    ACC --> ACC2["IP 白名單變更紀錄"]
    ACC --> ACC3["特權存取申請紀錄"]
    DEP --> DEP1["CAB 變更審核 全年"]
    DEP --> DEP2["k6 壓力測試報告 全年"]
    DEP --> DEP3["事故及回滾紀錄"]
    COMP --> COMP1["CIS Benchmark 稽核報告"]
    COMP --> COMP2["K8s RBAC 設定截圖"]
    COMP --> COMP3["Cilium Network Policy 設定"]
```

---

## 附錄：技術選型對照表

| 類別 | 工具 / 技術 | 版本基準 | 用途 |
|------|------------|---------|------|
| 作業系統 | Rocky Linux | 10 | 容器宿主機 |
| 虛擬化 | Hyper-V | Windows 11 | 實驗室環境 |
| 容器執行環境 | Docker CE + containerd | Latest Stable | 開發 + K8s CRI |
| 映像庫 | Harbor | 2.x | 私有 Registry |
| CI/CD | GitLab CE | Latest | Pipeline |
| 容器建置 | Kaniko | Latest | 無特權建置 |
| 安全掃描 | Trivy | 0.51+ | CVE/SBOM 掃描 |
| 運行時安全 | Falco | Latest | 異常偵測 |
| 映像簽章 | Cosign | Latest | 供應鏈安全 |
| 編排平台 | Kubernetes (kubeadm) | v1.31+ | 容器編排 |
| CNI | Cilium (eBPF) | Latest | 網路 + 安全 |
| LB | MetalLB | Latest | L4 負載平衡 |
| GitOps | ArgoCD | Latest | 自動化部署 |
| Policy | Kyverno | Latest | 准入控制 |
| API Gateway | APISIX | Latest | WAF / Auth |
| 機密管理 | Vault + ESO | Latest | 憑證管理 |
| 備份 DR | Velero | Latest | 容災備份 |
| 自動化 | Ansible | core | 組態管理 |
| 效能優化 | TCP BBR | Kernel 內建 | 網路吞吐量 |

---

> **文件審查週期：** 每年一次定期審查，或重大架構變更時立即更新
> **下次審查日期：** 2027-04-30
> **參考專案：** [enterprise-containerization-cases](https://github.com/tedtv1007-ctrl/enterprise-containerization-cases)
> **參考標準：** CIS Docker Benchmark / CIS Kubernetes Benchmark / NIST SP 800-190 / NSA Kubernetes Hardening Guide / SLSA Supply Chain Security